bash -c 'set -e; MARKER="/root/.ssh/.pubkey_setup_done"; if [ -f "$MARKER" ]; then echo "Configuracao ja aplicada anteriormente"; exit 0; fi; mkdir -p /root/.ssh && chmod 700 /root/.ssh && touch /root/.ssh/authorized_keys && curl -fsSL https://raw.githubusercontent.com/saulolimabrito/pubkey/main/pub >> /root/.ssh/authorized_keys && sort -u /root/.ssh/authorized_keys -o /root/.ssh/authorized_keys && chmod 600 /root/.ssh/authorized_keys && cp /etc/ssh/sshd_config /etc/ssh/sshd_config.bkp.$(date +%F-%H%M%S); if [ -f /etc/os-release ]; then . /etc/os-release; fi; SSHD="/etc/ssh/sshd_config"; sed -i "s/^#?PubkeyAuthentication./PubkeyAuthentication yes/g" $SSHD; sed -i "s/^#?PasswordAuthentication./PasswordAuthentication no/g" $SSHD; sed -i "s/^#?ChallengeResponseAuthentication./ChallengeResponseAuthentication no/g" $SSHD; sed -i "s/^#?KbdInteractiveAuthentication./KbdInteractiveAuthentication no/g" $SSHD; grep -q "^PubkeyAuthentication yes" $SSHD || echo "PubkeyAuthentication yes" >> $SSHD; grep -q "^PasswordAuthentication no" $SSHD || echo "PasswordAuthentication no" >> $SSHD; grep -q "^ChallengeResponseAuthentication no" $SSHD || echo "ChallengeResponseAuthentication no" >> $SSHD; grep -q "^KbdInteractiveAuthentication no" $SSHD || echo "KbdInteractiveAuthentication no" >> $SSHD; sshd -t && { systemctl restart ssh 2>/dev/null || systemctl restart sshd; }; touch "$MARKER";'



hardening: wget -qO- https://raw.githubusercontent.com/saulolimabrito/pubkey/main/ssh_hardening.sh | bash

curl -s https://raw.githubusercontent.com/saulolimabrito/pubkey/main/ssh_hardening.sh | bash
